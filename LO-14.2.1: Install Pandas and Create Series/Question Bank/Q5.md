Select the correct statements regarding the `pd.Series` constructor. (Select all that apply)

A. The `data` argument is mandatory; you cannot create an empty Series.
B. The `index` length must match the `data` length (unless data is a scalar).
C. Pandas will infer the `dtype` if it is not specified.
D. `name` is a mandatory argument.

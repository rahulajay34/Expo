import { ArchivesContent } from '@/components/features/archives/archives-content';

export const metadata = {
  title: 'Library | GCCP',
  description: 'Your saved content generations',
};

export default function ArchivesPage() {
  return <ArchivesContent />;
}
